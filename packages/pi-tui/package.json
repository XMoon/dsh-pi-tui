{
	"name": "@xmoon76/pi-tui",
	"version": "0.84.4",
	"private": true,
	"description": "Terminal User Interface library with differential rendering for efficient text-based applications (vendored fork of @earendil-works/pi-tui, see UPSTREAM.json)",
	"license": "MIT",
	"author": "Mario Zechner",
	"homepage": "https://github.com/XMoon/dsh-pi-tui#readme",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/XMoon/dsh-pi-tui.git",
		"directory": "packages/pi-tui",
		"note": "vendored snapshot of @earendil-works/pi-tui at v0.84.4 (commit b79e4cc834970cca69daebffab7df1da7d1e52c4, see UPSTREAM.json); carries the local divergences listed in packages/pi-tui/DIVERGENCES.md (re-verify all on every re-vendor)"
	},
	"keywords": [
		"tui",
		"terminal",
		"ui",
		"text-editor",
		"differential-rendering",
		"typescript",
		"cli"
	],
	"files": [
		"dist",
		"README.md"
	],
	"type": "module",
	"imports": {
		"#/*": "./src/*.ts"
	},
	"exports": {
		".": {
			"types": "./dist/index.d.mts",
			"default": "./dist/index.mjs"
		},
		"./package.json": "./package.json"
	},
	"scripts": {
		"build": "tsdown",
		"typecheck": "tsc -p tsconfig.json --noEmit",
		"test": "node --test test/*.test.ts",
		"clean": "rm -rf dist"
	},
	"dependencies": {
		"get-east-asian-width": "1.6.0",
		"marked": "18.0.5"
	},
	"devDependencies": {
		"@types/node": "^22.0.0",
		"@xterm/headless": "5.5.0",
		"chalk": "^5.4.1",
		"typescript": "^5.9.0",
		"tsdown": "0.22.0"
	},
	"engines": {
		"node": ">=22.19.0"
	},
	"//native": "native/ (darwin/win32 modifier-key prebuilds) is deliberately not vendored; loading degrades gracefully without it. See AGENTS.md."
}
