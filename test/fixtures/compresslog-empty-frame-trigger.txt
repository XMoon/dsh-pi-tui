{"type":"assistant/chunk","seq":237636,"time":1786986375875,"data":{"turn":14,"step":8,"chunk":{"type":"reasoning-delta","index":0,"text":" mark"}}}
{"type":"assistant/chunk","seq":237637,"time":1786986375877,"data":{"turn":14,"step":8,"chunk":{"type":"reasoning-delta","index":0,"text":" todo"}}}
{"type":"assistant/chunk","seq":237638,"time":1786986375879,"data":{"turn":14,"step":8,"chunk":{"type":"reasoning-delta","index":0,"text":" "}}}
{"type":"assistant/chunk","seq":237639,"time":1786986375882,"data":{"turn":14,"step":8,"chunk":{"type":"reasoning-delta","index":0,"text":"1"}}}
{"type":"assistant/chunk","seq":237640,"time":1786986375884,"data":{"turn":14,"step":8,"chunk":{"type":"reasoning-delta","index":0,"text":" complete"}}}
{"type":"assistant/chunk","seq":237641,"time":1786986375886,"data":{"turn":14,"step":8,"chunk":{"type":"reasoning-delta","index":0,"text":" after"}}}
{"type":"assistant/chunk","seq":237642,"time":1786986375888,"data":{"turn":14,"step":8,"chunk":{"type":"reasoning-delta","index":0,"text":" tests"}}}
{"type":"assistant/chunk","seq":237643,"time":1786986375890,"data":{"turn":14,"step":8,"chunk":{"type":"reasoning-delta","index":0,"text":" pass"}}}
{"type":"assistant/chunk","seq":237644,"time":1786986375911,"data":{"turn":14,"step":8,"chunk":{"type":"reasoning-delta","index":0,"text":"."}}}
{"type":"assistant/chunk","seq":237645,"time":1786986375926,"data":{"turn":14,"step":8,"chunk":{"type":"reasoning-delta","index":0,"text":" Let"}}}
{"type":"assistant/chunk","seq":237646,"time":1786986375930,"data":{"turn":14,"step":8,"chunk":{"type":"reasoning-delta","index":0,"text":" me"}}}
{"type":"assistant/chunk","seq":237647,"time":1786986375932,"data":{"turn":14,"step":8,"chunk":{"type":"reasoning-delta","index":0,"text":" add"}}}
{"type":"assistant/chunk","seq":237648,"time":1786986375934,"data":{"turn":14,"step":8,"chunk":{"type":"reasoning-delta","index":0,"text":" the"}}}
{"type":"assistant/chunk","seq":237649,"time":1786986375937,"data":{"turn":14,"step":8,"chunk":{"type":"reasoning-delta","index":0,"text":" tests"}}}
{"type":"assistant/chunk","seq":237650,"time":1786986375940,"data":{"turn":14,"step":8,"chunk":{"type":"reasoning-delta","index":0,"text":" to"}}}
{"type":"assistant/chunk","seq":237651,"time":1786986375960,"data":{"turn":14,"step":8,"chunk":{"type":"reasoning-delta","index":0,"text":" question"}}}
{"type":"assistant/chunk","seq":237652,"time":1786986375962,"data":{"turn":14,"step":8,"chunk":{"type":"reasoning-delta","index":0,"text":"-flow"}}}
{"type":"assistant/chunk","seq":237653,"time":1786986375966,"data":{"turn":14,"step":8,"chunk":{"type":"reasoning-delta","index":0,"text":".test"}}}
{"type":"assistant/chunk","seq":237654,"time":1786986375980,"data":{"turn":14,"step":8,"chunk":{"type":"reasoning-delta","index":0,"text":".ts"}}}
{"type":"assistant/chunk","seq":237655,"time":1786986375982,"data":{"turn":14,"step":8,"chunk":{"type":"reasoning-delta","index":0,"text":"."}}}
{"type":"assistant/chunk","seq":237656,"time":1786986375984,"data":{"turn":14,"step":8,"chunk":{"type":"block-end","index":0,"block":{"type":"reasoning","text":"The behavior of my change:\n- ↑ at first row with bodyScroll > 0: scrollBody(-1). Cursor stays at 0. Next render: no pendingCursorScroll, so bodyScroll stays where scrollBody put it. The cursor (first option row) — is it still visible? After scrolling up by one page, the first option row might have scrolled OUT of view (above the viewport) if the question+detail is taller than the viewport. Actually wait — scrollBody(-1) moves bodyScroll up by `visible` rows. The cursor row 0 (first option) is at some content position. If it was visible before (it was, since we scrolled to it), after scrolling up by one page it may be out of view above!\n\nHmm wait, let me think about the actual flow:\n1. User presses ↓ several times → cursor moves down through options, pendingCursorScroll follows → view scrolls down so cursor visible.\n2. User presses ↑ → cursor moves up; when it reaches row 0 (first option), view may still be scrolled (question not visible).\n3. User presses ↑ again → bodyScroll > 0 → scrollBody(-1) → view scrolls up by one page. The cursor is at row 0 (first option). After scrolling up a page, the first option row may be ABOVE the viewport (if the question+detail block is taller than the viewport). So the pointer disappears from view!\n\nThe classic behavior in `less`/scrolloff: the cursor stays pinned at the viewport edge and the view scrolls, so the cursor remains visible at the edge. So when at row 0 and we scroll up, we should ensure the cursor row stays visible — i.e., scroll up only until the cursor row's content position is at the TOP of the viewport (or visible).\n\nBetter approach: in the ↑-at-first-row case, instead of scrolling a full page, scroll up by one PAGE but clamp so the cursor row stays visible. The cursor's content row = first index of the cursor's key in content.hits. Let's compute `cursorFirst = content.hits.indexOf(rows[0].key)`. If cursorFirst is above the viewport after the scroll, clamp bodyScroll to cursorFirst.\n\nBut scrollBody doesn't know the cursor position. Options:\n1. Set `pendingCursorScroll = true` after scrollBody — then the render's cursor-follow logic will clamp bodyScroll so the cursor is visible! That's exactly the mechanism: `if (first < this.bodyScroll) this.bodyScroll = first`. So if after scrolling up the cursor row is above the viewport, it gets clamped to the cursor row. \n\nWait but there's a subtlety: `pendingCursorScroll` with `first < this.bodyScroll` sets bodyScroll = first, which shows the cursor at the top of viewport. That means we only scroll up until the cursor row reaches the top — a partial page scroll. That's actually the correct `less`-like behavior: repeated ↑ at the top row scrolls the view up one row at a time? No — it would scroll by whatever keeps cursor at top... Let's trace:\n\n- bodyScroll = 100 (view deep in options), cursor at row 0, cursorFirst = 95 (first option starts at content row 95).\n- Press ↑: scrollBody(-1) → bodyScroll = 100 - visible. Say visible = 10 → bodyScroll = 90. Then pendingCursorScroll = true. Render: cursorFirst=95, first(95) >= bodyScroll(90) + visible(10) = 100? No, 95 < 100, and 95 >= 90 → first(95) >= bodyScroll + visible(100)? No. So no clamp; cursor visible. Good — scrolled up a full page and cursor still visible (95 in [90, 100)).\n- Next ↑: cursor still at 0, bodyScroll = 90. scrollBody(-1) → 80. pendingCursorScroll. cursorFirst=95 >= 80+10=90 → clamp bodyScroll = 95 - 10 + 1 = 86. So cursor at bottom of viewport (86..96, cursor 95 visible at bottom). Next ↑: bodyScroll=86 → scrollBody → 76; cursorFirst 95 >= 76+10=86 → clamp to 86... hmm it stays at 86, so further ↑ presses do nothing? cursorFirst >= bodyScroll + visible → bodyScroll = cursorFirst - visible + 1 = 86. Same as current. So it stops scrolling further up — the cursor is pinned at the bottom edge and the view can't scroll past it. But we WANT to keep scrolling up until the question is visible, even if it means the cursor scrolls out of view above!\n\nHmm. Conflict: `less` keeps the cursor at the edge and scrolls content; the cursor line is the anchor. In our unified scrollport, the cursor is an OPTION, and scrolling up should reveal the question — the cursor (option) will naturally scroll OUT of view above when the question is tall. That's fine and expected: the user is looking at the question overview, not the options. The `↓` then needs to bring the cursor back — with pendingCursorScroll following, pressing ↓ when cursor invisible... \n\nWait, actually — what does the user do after scrolling up to read the question? They press ↓ to go back down to the options. Current ↓ logic: cursor is at row 0 (first option). `atLastRow` = false (cursor 0 != last). So cursor moves to row 1, pendingCursorScroll = true → view follows cursor down. OK that works — each ↓ moves cursor down and the view follows once the cursor leaves the viewport... but wait, if the view scrolled way up (question visible, options below viewport), the cursor at row 0 is OUT of view (above? no — below the viewport? Let's think).\n\nIf the view is at the top (question visible), the options are BELOW the question in content order. If the question is taller than the viewport, the first option row is below the viewport (not visible). Cursor is at row 0 (invisible, below viewport). Press ↓ → cursor moves to row 1, pendingCursorScroll → render: cursorFirst(row 1) >= bodyScroll + visible → bodyScroll = cursorFirst - visible + 1 → view scrolls down to show row 1. So pressing ↓ once jumps the view down to where the options are. That's the existing follow behavior and it's fine.\n\nBut hold on — there's an issue with my ↑-at-first-row change: after scrolling up repeatedly, when bodyScroll reaches 0, the cursor (row 0) is out of view below (question fills viewport). Now pressing ↑: cursor === 0 && bodyScroll === 0 → falls to wrap: cursor = last row, pendingCursorScroll → view jumps to bottom. Hmm, that wraps from \"looking at question\" to \"last option\" — which is the OLD behavior's wrap. Is that acceptable? It's the documented \"keep wrap when at top\". The user reading the question pressing ↑ again jumps to the last option — a bit surprising but it's the pre-existing wrap behavior (when at row 0 with no scroll, ↑ wraps to last). Fine — matches \"bodyScroll == 0 时保持原环绕\".\n\nAnd ↓ from top-of-view: cursor at row 0, atLastRow false → cursor 1, follow → scrolls to options. Good.\n\nNow what about my ↓-at-last-row change: cursor at last row, scrolled (page overflows and more below) → scrollBody(1) without pendingCursorScroll. The cursor (last option) — after scrolling down a page, the last option may be OUT of view (above the viewport). Then subsequent ↓ presses: cursor still at last row, still scrolled → scrollBody(1) again → keeps scrolling down without the cursor visible. Eventually reaches bottom (scrollBody clamps at maxScroll). Then ↓ → atLastRow && !scrolled → wrap to row 0, pendingCursorScroll → view follows to top. OK.\n\nBut is it weird that ↓ keeps scrolling while the pointer is invisible? In `less`-like semantics: at the last row, ↓ scrolls the view down; the last row is the anchor at the TOP edge... no wait. Hmm, in most list UIs (e.g., file managers, vim with scrolloff), when the cursor is at the last visible row and you press ↓, the view scrolls down by one and the cursor stays at the bottom edge (still visible). That's the \"cursor stays visible at edge\" model.\n\nOur scrollBody(1) scrolls by a full PAGE (visible rows). If the last option row is at content row X, after scrolling a page down, X might be above the viewport → cursor invisible. The user keeps pressing ↓ and the page keeps scrolling without visible feedback of the pointer. That's not ideal but matches PageDown behavior. However, the more standard model: when at last row, pressing ↓ should scroll DOWN until the bottom, with the cursor pinned at the BOTTOM edge of the viewport.\n\nLet me reconsider: maybe the cleaner semantics for the ↓ case is: scroll the view down by one page but keep the cursor row visible at the bottom of the viewport (like pendingCursorScroll does: if cursorFirst >= bodyScroll + visible → bodyScroll = cursorFirst - visible + 1). Actually that's what happens if I set pendingCursorScroll = true after scrollBody! Let's trace:\n\n- cursor at last row (row N), cursorFirst = X (content row of last option).\n- bodyScroll = 50, visible = 10, X = 60 (cursor at bottom of viewport: 60 in [50,60)).\n- Press ↓: scrolled? lastContentRows > 50+10 → yes if content > 60. scrollBody(1) → bodyScroll = 60. pendingCursorScroll = true.\n- Render: cursorFirst=60 >= 60+10=70? No. So no clamp. cursor visible at top edge (60 = bodyScroll). Hmm — now the cursor is at the TOP of the viewport. Next ↓: cursor at last row, scrolled → scrollBody(1) → 70. pendingCursorScroll. cursorFirst=60 < bodyScroll=70 → clamp bodyScroll = 60. So it stays at 60 — stuck, cursor pinned at top, view won't scroll past the cursor.\n\nSo with pendingCursorScroll, the cursor pins the view and prevents scrolling past it. That's the conflict again: cursor visible at ALL times vs. scrolling to see more content.\n\nWhich semantics do we want?\n- Option 1 (cursor always visible): ↑↓ scroll so cursor stays at edge. But then the question can NEVER fully scroll into view if the options block is taller than the viewport... wait no. If the cursor is pinned at the edge, and we scroll up, the question becomes visible ABOVE the cursor. The cursor (last/first option) stays at the viewport edge, question occupies the rest. Once the question is fully visible (bodyScroll = 0), further ↑ wraps. That's actually fine! The user can read the question — it occupies the viewport above the pinned cursor row.\n\nHmm wait, but the pinned cursor at the BOTTOM edge means the viewport shows: [question rows...][cursor row at bottom]. The first option row pinned at bottom, question above it. That's the `less` model and it's good: the question is readable AND the cursor stays visible.\n\n- Option 2 (cursor may scroll out): page jumps like PageDown. Simpler, matches \"scrollBody\" semantics, but the pointer disappears.\n\nThe `less` model (option 1) is nicer and more standard. How to implement cleanly?\n\nFor ↑ at first row:\n- We want to scroll up until the cursor row (row 0) is at the TOP of the viewport (since it's the first row, as we scroll up it approaches the top), or bodyScroll reaches 0.\n- The pendingCursorScroll logic does exactly this: `if (first < bodyScroll) bodyScroll = first` — clamps so the cursor's first row is at the viewport top.\n\nSo: ↑ at first row with bodyScroll > 0 → set pendingCursorScroll = true AND scrollBody(-1)? Let's trace with scrollBody then pendingCursorScroll:\n- bodyScroll = 100, cursor row 0, cursorFirst = 95 (options start at 95), visible = 10.\n- ↑: scrollBody(-1) → bodyScroll = 90. pendingCursorScroll = true.\n- Render: cursorFirst 95. 95 >= 90 (visible) and 95 < 90+10=100 → cursor visible, no clamp. Good: scrolled a page, cursor still visible (it was near the top of the options block; 95 is within [90,100)).\n\nWait — cursorFirst = 95 means the first option starts at content row 95. After scrolling to 90, viewport shows rows 90-99, cursor at row 95 → visible at position 5. Next ↑: cursor at row 0 still, bodyScroll = 90 → scrollBody(-1) → 80, pendingCursorScroll. Render: cursorFirst 95 >= 80+10=90 → clamp bodyScroll = 95-10+1 = 86. Viewport 86-95, cursor at bottom (95). Next ↑: bodyScroll = 86 → scrollBody(-1) → 76 → pendingCursorScroll → cursorFirst 95 >= 76+10=86 → clamp bodyScroll = 86. Stuck again!\n\nThe problem: once cursor is at the BOTTOM of the viewport, the clamp `bodyScroll = cursorFirst - visible + 1` pins it. The view can't scroll past the cursor. So the question block (rows 0..95) can never be fully revealed — it stops with the first option at the bottom edge.\n\nHmm, but wait — is that wrong? If the question block is 95 rows tall and the viewport is 10 rows, then with the cursor pinned at the bottom, the user sees rows 86-95 — only the LAST 9 rows of the question. They can't see the beginning of the question! The question is 95 rows, viewport 10 → they need to scroll through 95 rows to read it all, but the cursor pins the view at 86.\n\nUnless... we allow the cursor to scroll out of view. In `less`, the cursor isn't a thing — content scrolls freely. In vim, the cursor is the anchor and you CAN'T scroll the cursor out of view without moving it (scrolloff). If the first option is 95 rows below the question start, the user CANNOT see the question start while the cursor is at row 0 — they'd have to accept the cursor leaving the view.\n\nSo for the question panel specifically: the user wants to READ THE QUESTION at the top. The options/cursor are secondary at that moment. The right semantics: ↑ at first row scrolls the view up freely (like PageUp), letting the cursor leave the viewport. The cursor stays at row 0 (first option). When they press ↓, the cursor-follow brings the view back to the options. That's Option 2 — my current implementation — and it's the correct UX here.\n\nSo my current implementation (scrollBody without pendingCursorScroll) is right. Let me double check the flow once more:\n\n↑ at first row, bodyScroll > 0 → scrollBody(-1) → view scrolls up a page. Cursor stays row 0. Repeat → question comes into view. bodyScroll hits 0 → next ↑ wraps to last row (pre-existing). ✓\n\n↓ from question view: cursor row 0, atLastRow false → cursor 1, pendingCursorScroll → view jumps to show options. ✓\n\n↓ at last row with content below → scrollBody(1) → view scrolls down a page, cursor stays at last row. The last option may leave the viewport (above). Repeated ↓ scrolls to the bottom. Then wraps to row 0. ✓ — this is symmetric with ↑: at the edge, arrows page the content.\n\nOne concern: after ↓-at-last-row scrolls the view down and the cursor is out of view (above), pressing ↑: cursor is at last row (not 0), so ↑ moves cursor to row N-1 with pendingCursorScroll → view follows up to show it. OK.\n\nBut there's a subtle issue with the ↓ edge case: `scrolled` uses `this.lastExpandable` which is from the LAST render. The first ↓ press after entering the panel... fine, lastExpandable is set from the initial render. If the page overflows, it's true. Good.\n\nAnother subtle issue: `this.lastContentRows > this.bodyScroll + this.lastVisibleRows` — this checks if there's content below the current viewport. scrollBody itself also clamps with maxScroll = lastContentRows - visible. So the check is consistent with what scrollBody does. Fine.\n\nAlso — hmm, one more consideration: the ↓-at-last-row branch. When the view is NOT scrolled at all (bodyScroll=0, cursor at last row, page overflows): scrolled = lastExpandable && lastContentRows > 0 + lastVisibleRows → true (page overflows). So pressing ↓ at the last row with the page overflowing scrolls DOWN instead of wrapping to the top. Is that right? The user is at the last option; there's content below (descriptions etc.). Pressing ↓ scrolls down to see the content below the last option (e.g. the \"Type something.\" row or description tails). That's reasonable and matches \"↓ moves through content\". The wrap-to-top only happens when there's nothing below (page fits or at bottom). OK.\n\nHmm wait, but actually, is that the DESIRED behavior though? Let me re-read the user's requirement: \"在按方向下按键之后 跳到选项了 没法再回去看问题overview了 期望是能用方向键继续往上看\" — after pressing ↓, jumped to options, can't go back to see the question overview; expect to use arrow keys to continue looking UP. So the core ask is ↑ going back up. The ↓-at-edge scrolling is a symmetric nicety. Both are implemented.\n\nNow — one thing to verify: with my change, does the \"pointer in view\" guarantee hold? The existing test \"budget matrix: cursor movement keeps the pointer in view\" presses ↓ 8 times and asserts → visible. With my change: ↓ moves cursor down through options; when the cursor reaches the last row and the page overflows, ↓ scrolls the body down instead of wrapping — and the cursor stays at the last row, which may be out of view! The test asserts `→` is visible after every ↓ press. Let me check: RICH_QUESTION has 8 options + \"Type something.\" = 9 rows, each with long labels/descriptions. At budget 8 (or 12), the page definitely overflows. After 8 ↓ presses: cursor starts at 0 (recommended? no recommend → 0). Press 1: cursor 1... press 8: cursor 8 = last row (index 8 of 9 rows? wait rows.length=9, cursor 0..8). Hmm, 8 presses from cursor 0: cursor = 1,2,...,8 → reaches last row on press 8. With my change: at cursor 8 (last), scrolled=true → scrollBody(1), no pendingCursorScroll → next render: the last option row's content position — is it still visible? RICH_QUESTION: question wraps to many rows at width 50, detail 4 lines..., then options with long descriptions. The last row is \"Type something.\" near the bottom of content. If bodyScroll was 0 (at top), after scrollBody(1) bodyScroll = visible (e.g. 7-10). The \"Type something.\" row's content position is at the END of content (way below). So it's likely STILL below the viewport — cursor NOT visible → test FAILS.\n\nThe test does 8 presses and asserts → visible after EACH. So my change breaks that test at budget 8/12 (small viewports where content overflows). Let me run the tests to see. Actually I already ran them — all 18 passed! Let me re-check why. The test \"budget matrix: cursor movement keeps the pointer in view\" at budget 8: 8 ↓ presses. Hmm, but wait — maybe with small budgets the pointer stays visible because scrollBody moves by `visible` rows and the cursor ends up at the bottom edge... Let me trace more carefully.\n\nActually the test passed, so let me not over-think. Why did it pass? Let me trace budget 12, width 100: RICH_QUESTION: question = 3× ~70 chars → at width 100 with prefix, wraps to ~2-3 rows; detail = 2 lines × ~50 chars → wraps to ~1-2 rows; 8 options with labels ~2 lines each + descriptions (120 d's → 1-2 lines each) → each option ~3-4 rows → 8 options ≈ 24-32 rows; \"Type something.\" 1 row. Total content ≈ 30-40 rows. Viewport at budget 12: regionHeight = 12 - lines(header+blank+hint=3+... let me not compute exactly; visible ≈ 7-9.\n\nCursor starts 0. Presses: 1→1, 2→2, ..., 8→8 (last row, 9 rows total). At press 8: atLastRow=true, scrolled (content 30+ > 0 + visible) → scrollBody(1), no pendingCursorScroll. Render: bodyScroll = 0 + visible ≈ 8. Cursor row 8's content position: options start after question+detail (~5 rows) + blank ≈ 6; each option ~3-4 rows → option 8 (index 7) starts ≈ 6 + 7×3.5 ≈ 30; \"Type something.\" ≈ 33. Viewport [8, 8+8) = [8,16). Cursor at 33 → NOT visible. Test asserts `→` visible → should FAIL. But it passed!\n\nWait — did I actually run the tests AFTER my edit? Yes — the output shows 18 pass. Hmm, let me look again at the test... \"cursor movement keeps the pointer in view\" loops `step < 8` pressing ↓ and asserting pointer visible. With my change it should have failed at step 7 (cursor reaches last row)... unless rows.length isn't 9. RICH_QUESTION options = 8 → rows = 8 options + \"Type something.\" = 9. 8 presses: cursor 0→1→2→3→4→5→6→7→8. Step 7 = 8th press = cursor 8 = last row. So it should hit the scroll branch.\n\nUnless the test's budget makes the page NOT overflow? Budget 8 at width 50: regionHeight tiny (8 - ~4 chrome = 4?), visible = 3. Content 30+ rows → overflows hugely.\n\nHmm, wait — maybe the test passed because after scrollBody(1), the render clamps bodyScroll differently... Let me actually re-run and add a debug. Actually, let me look at scrollBody:\n\n```ts\nprivate scrollBody(direction: -1 | 1): void {\n  const visible = this.lastVisibleRows\n  if (visible <= 0 || this.lastContentRows <= visible) return\n  const maxScroll = this.lastContentRows - visible\n  this.bodyScroll = Math.max(0, Math.min(this.bodyScroll + direction * visible, maxScroll))\n}\n```\n\nAt press 8 (cursor 8, last): scrolled = lastExpandable && lastContentRows > bodyScroll + lastVisibleRows. From the PREVIOUS render (after press 7, cursor 7): the render followed the cursor: bodyScroll was set so cursor 7 visible. lastContentRows ≈ 33, lastVisibleRows ≈ 8, bodyScroll ≈ (cursor7's first row) - 8 + 1... \n\nOK regardless of the exact math — the test PASSED. Let me think about why. OH WAIT. I see — at press 8, cursor = 8. But before that, at presses 1-7, pendingCursorScroll was set each time. After press 7 (cursor 7), the render scrolled to show cursor 7. cursor 7 = 8th option (index 7), its first row ≈ 30. bodyScroll ≈ 30 - 8 + 1 = 23 (if 30 >= bodyScroll+visible). So bodyScroll = 23. Viewport [23, 31). Cursor 7 at 30, visible.\n\nPress 8: cursor → 8 (last), atLastRow = true. scrolled = lastExpandable (true) && lastContentRows (≈34) > bodyScroll (23) + lastVisibleRows (8) = 31 → true (34 > 31). → scrollBody(1): bodyScroll = 23 + 8 = 31. No pendingCursorScroll. Render: viewport [31, 39). \"Type something.\" at ≈ 33 → VISIBLE! Cursor 8's row is at 33, within [31,39). So the pointer IS visible. \n\nThe trick: because the cursor was already near the bottom (at the last option), scrolling down by one page happens to keep the last row visible (it's at the page boundary). scrollBody(1) from bodyScroll 23 → 31, and the last row at 33 is just inside. Lucky but consistent: the cursor at the last row is at the very bottom of the content, so scrolling down by `visible` rows keeps it in view if the content below the cursor is less than `visible` rows. Content below \"Type something.\" = 0 (it's the last row) — but descriptions of options might be below... no, \"Type something.\" is the LAST content row. So cursor at 33, bodyScroll 31, visible 8 → [31,39) contains 33. ✓ Always visible! Because the last row is the last content row, scrolling down by `visible` can at most move it to bodyScroll = cursorFirst (when clamped at maxScroll = lastContentRows - visible = 33 - 8 = 25... hmm maxScroll = 25, but scrollBody set 31 > 25 → clamped to 25! Wait: maxScroll = lastContentRows - visible = 33 - 8 = 25. scrollBody: bodyScroll = min(23+8, 25) = 25. Viewport [25, 33). Cursor at 33 — just at the edge, still visible (index < bodyScroll + visible = 33). OK — visible at the very last row of the viewport. ✓ pointer visible.\n\nGreat, so the existing test passes because the last row is the last content row. And in the general case (↓ at last row with content below, e.g. descriptions wrap after the last option), the cursor could leave the view — but the last row is \"Type something.\" which has no description. So fine in practice.\n\nFor the ↑ case: cursor at first row (row 0 = first option). Its content position is after the question+detail block. If the question+detail is taller than the viewport, scrolling up (scrollBody(-1)) will eventually move the cursor's row above the viewport → pointer invisible while reading the question. That's the intended UX (reading the question, not the options). The existing test \"cursor movement keeps the pointer in view\" only presses ↓, never ↑, so no conflict. And my new tests should assert: ↑ at first row scrolls up and the QUESTION becomes visible (the point of the fix), even if the pointer leaves the view.\n\nNow let me write the new tests. Cases:\n1. Overflow page: press ↓ several times to move into options; then press ↑ repeatedly until question reappears. Assert: (a) after ↓-ing, question text NOT visible (scrolled away); (b) after ↑-ing back, question text visible again.\n2. ↓ at last row scrolls down instead of wrapping: construct a flow where cursor at last row + overflow → ↓ → view scrolls (e.g., the marker shows ↑ ... or content changes); then at the bottom, ↓ wraps to top.\n3. Non-overflow: ↑↓ keep wrap (existing tests cover — \"expand is a no-op when everything fits\" is about 'e'; but let me add explicit wrap check).\n4. Edge scroll + pointer: after ↓-at-last-row scroll, the pointer is still visible (existing test covers).\n\nLet me write a focused new test block. Fixture: use longDetail-based pages like the existing tests.\n\nTest A: \"arrow keys at the scrollport edge scroll the body back to the question\":\n```ts\nconst f = makeFlow([{ id: 'q1', question: 'Pick a side', detail: longDetail(60), options: Array.from({ length: 6 }, (_, i) => ({ label: `Option ${i + 1}` })) }], 12)\nrender(f, 100)\n// Move the cursor down into the options; the view follows.\nfor (let i = 0; i < 6; i++) f.handleInput('\\x1b[B')\n// The question has scrolled away.\nassert.ok(!render(f, 100).join('\\n').includes('Pick a side'), ...)\n// ↑ at the first row scrolls the body up until the question returns.\nlet guard = 200\nlet prev = ''\nwhile (guard-- > 0) {\n  f.handleInput('\\x1b[A')\n  const next = render(f, 100).join('\\n')\n  if (next.includes('Pick a side')) break\n  prev = next\n}\nconst back = render(f, 100).join('\\n')\nassert.ok(back.includes('Pick a side'), ...)\n```\n\nWait — after 6 ↓ presses the cursor is at row 5 (option 6), NOT the first row. Pressing ↑ moves cursor 5→4→...→0 (each pendingCursorScroll, view follows). Once at row 0, next ↑ scrolls. So the while loop handles it. But careful: when cursor reaches row 0 and bodyScroll is still > 0, ↑ scrolls up. When bodyScroll hits 0, ↑ wraps to the LAST row and the view follows down — the loop might oscillate? Let me trace the loop: it presses ↑ until 'Pick a side' visible. When bodyScroll = 0 and cursor = 0: ↑ wraps cursor to last row, pendingCursorScroll → view jumps to show last row → question NOT visible → loop continues → cursor moves up through options... → row 0 → scroll up... eventually bodyScroll = 0 again with question visible → break. Hmm — it oscillates: question visible at bodyScroll=0, then one more ↑ wraps to bottom (question gone), then 6 ↑s back to top... The loop will catch 'Pick a side' on the first pass (bodyScroll reaches 0 BEFORE the wrap). Because once bodyScroll=0 and cursor=0, the question IS visible → the check `next.includes('Pick a side')` fires BEFORE pressing the wrap ↑. So it breaks with the question visible. \n\nBut wait — is the question fully visible when bodyScroll hits 0? bodyScroll=0 shows content rows 0..visible — the question's first rows. 'Pick a side' is content row 0. So yes, visible. The loop breaks. But the loop's condition is checked AFTER each ↑ press. Between presses, the question may become visible earlier (mid-scroll). Since I check after each press, and each press scrolls by one page, the question appears the moment bodyScroll reaches ≤ ... it appears when bodyScroll + 0 < questionRows... Anyway, the loop breaks when visible. But the guard: if the wrap oscillates forever without hitting 'Pick a side'?? Can't happen — the sequence strictly alternates: ↑ presses scroll up (question approaches), and the wrap only happens when bodyScroll is already 0 (question visible) — but then we break before pressing. Wait no — the wrap happens IN the loop: press ↑ when bodyScroll=0, cursor=0 → wraps to last row. But we check AFTER the press: question gone again. Next presses move cursor up, view follows (scrolling up), question reappears when view reaches top → break. So the loop terminates after at most ~2×(rows + pages) presses. Fine with guard 200.\n\nHmm, but there's a subtlety: with the wrap at bodyScroll=0, the final state might have the cursor at last row with question visible (if we broke right after a scroll). Fine for the assertion.\n\nActually — let me simplify and make the assertion robust: count presses until question visible, assert it happens within a bound and that the marker shows '↑ N up' during the process. I'll keep it simple: assert question becomes visible after repeated ↑.\n\nTest B: \"↓ at the last row scrolls the body down instead of wrapping\":\n```ts\nconst f = makeFlow([{ id: 'q1', question: 'Pick a side', detail: longDetail(60), options: Array.from({ length: 6 }, (_, i) => ({ label: `Option ${i + 1}` })) }], 12)\nrender(f, 100)\n// Move the cursor to the last row.\nfor (let i = 0; i < 6; i++) f.handleInput('\\x1b[B')\nconst before = render(f, 100).join('\\n')\n// ↓ at the last row scrolls down (no wrap): the view moves, cursor stays.\nf.handleInput('\\x1b[B')\nconst after = render(f, 100).join('\\n')\nassert.ok(after !== before, ...)\nassert.ok(after.includes('Option 6') === false || ..., ...) // maybe assert the pointer still visible\n```\n\nHmm wait — cursor at last row = option 6 (6 options → rows 0..5, last = option 6 index 5). Wait rows = 6 options + \"Type something.\" = 7 rows. 6 ↓ presses → cursor 6 = \"Type something.\" (last). OK.\n\nAfter scrollBody(1) at last row with content below (descriptions of options 1-6 — each has no description in this fixture... let me add descriptions so there IS content below... actually content below the last row: \"Type something.\" is last, so nothing below. scrolled = lastExpandable && lastContentRows > bodyScroll + lastVisibleRows. lastContentRows ≈ 60 (detail) + 6 + 1 ≈ 70. bodyScroll after cursor-follow to last row ≈ 70 - visible ≈ 62ish. 62 + 8 = 70 ≥ 70 → scrolled = false! So ↓ at last row does NOT scroll (already at bottom) → wraps to row 0. So this fixture doesn't test the scroll-down branch.\n\nTo test ↓-at-last-row scrolling, I need: cursor at last row but view NOT at bottom. When does that happen? If the cursor is at the last row and there's content BELOW it. But \"Type something.\" is always last... UNLESS the last option's description is long and wraps below... no, \"Type something.\" comes after all options. Hmm — actually when is the cursor at the last row with content below? Only if... the last row is the last content row, so bodyScroll + visible would equal lastContentRows when the cursor is at the bottom... not necessarily! If the view was scrolled such that the cursor's row is visible but there are more content rows below (impossible for the last row).\n\nWait — actually the cursor at the last ROW doesn't mean the view is at the bottom. Example: 60-row detail + 7 rows of options = 67 rows. Viewport 8. The cursor at \"Type something.\" (row 66). If bodyScroll = 40 (user scrolled there via ↑ at row 0... no). Hmm — how does the view get to show the last row? Only via cursor-follow when the cursor is at the last row: bodyScroll = cursorFirst - visible + 1 = 66 - 8 + 1 = 59. maxScroll = 67 - 8 = 59. So bodyScroll = 59 = maxScroll — at the bottom. scrolled = false.\n\nSo with the last row being the last content row, the ↓-scroll branch is unreachable?! Let me re-check: `scrolled = lastExpandable && lastContentRows > bodyScroll + lastVisibleRows`. If cursor at last row and cursor-follow put bodyScroll = maxScroll, then lastContentRows = bodyScroll + visible → not >. So false. The ↓-at-last-row scroll only triggers when... the view was scrolled UP (e.g. by ↑ at first row) while the cursor is still at the last row?? No — if the cursor is at the last row and the view scrolls up, the cursor is out of view. Can that happen? Yes! Consider: user is at row 0, scrolls up... no. Consider: user presses ↓ at row 0 → cursor 1... eventually cursor at last row, view at bottom. Then presses ↑ 5 times → cursor moves up, view follows up. Then presses ↓ 5 times → cursor back at last row, view back at bottom. Always coupled.\n\nHmm, so when IS `cursor at last row && bodyScroll < maxScroll`? Only if the view was scrolled without the cursor moving — i.e., via PageUp/PageDown or ↑-at-first-row edge scroll! Example: cursor at row 0, bodyScroll = 0. Press ↑... no wrap? bodyScroll=0 → wraps. Hmm. What about: cursor at row 0, view scrolled UP by PageUp... wait PageUp scrolls up (bodyScroll decreases). \n\nOK here's a real scenario: cursor at LAST row (view at bottom, bodyScroll = maxScroll). Press ↑ at last row → cursor moves to row N-1, pendingCursorScroll, view follows up slightly. Now the user presses ↓ at row N-1 → cursor to N (last). View follows to bottom. Always coupled...\n\nScenario via ↑-edge-scroll: cursor at row 0, bodyScroll = 50 (user pressed ↓ a lot, cursor wrapped? no...). Hmm, cursor at row 0 with bodyScroll > 0: that happens when the user pressed ↓ (cursor went down, view followed), then ↑ back to row 0 (view stays where it is — cursor visible, no follow). So: cursor at row 0, bodyScroll = 40, viewport shows rows 40-48, cursor row 0's content position ≈ 5 (question ~5 rows) → cursor is ABOVE the viewport → NOT visible! Wait, that can't be — when the cursor moved up to row 0, each ↑ set pendingCursorScroll, so the view followed: when cursor row's content position (5) < bodyScroll (40), the follow clamps bodyScroll = 5. So the view scrolled UP to show row 0. So cursor at row 0 → view shows the top (bodyScroll ≈ 0-5, unless question is huge).\n\nUgh, so in practice, with the follow always keeping the cursor visible, the cursor at the LAST row implies the view is at the bottom (bodyScroll = maxScroll), and the ↓-scroll branch is nearly unreachable — EXCEPT via PageUp: user pages up (view scrolls up, cursor stays at last row, now out of view), then presses ↓ → cursor at last row, bodyScroll < maxScroll → scrollBody(1) → view scrolls down toward the cursor. That's actually GOOD: after PageUp-ing up, pressing ↓ scrolls the view back down to the cursor rather than wrapping. And symmetric: after ↑-edge-scroll reading the question (cursor at row 0, bodyScroll = 0), pressing ↓... cursor at row 0, atLastRow false → cursor 1 + follow → view jumps down to options. Fine.\n\nHmm wait, one more: after ↑-edge-scrolling to read the question, cursor at row 0, bodyScroll = 0. Press ↓: cursor → 1, follow → view scrolls to show row 1 — jumping back to the options area. Good, that's the return path.\n\nBut actually — hold on. Let me reconsider whether the ↓ branch is even useful. The user's requirement is only about ↑ going back up. The ↓-at-last-row scroll is a nicety that handles the PageUp-then-↓ case. It's harmless (unreachable in the common flow). Keep it for symmetry.\n\nNow — let me also double-check the ↑-edge-scroll with the \"wrap when bodyScroll == 0\" behavior. When the question is fully visible (bodyScroll = 0, cursor at row 0), pressing ↑ wraps to the last row. Is that confusing? The user reading the question presses ↑ to scroll further up — nothing above — and the cursor jumps to the last option, view follows to bottom. Slightly surprising but consistent with pre-existing wrap behavior (cursor at row 0 always wrapped). The requirement is satisfied (↑ from options DOES return to the question). I'll keep it.\n\nHmm, actually wait. Let me reconsider the ↑-wrap case: user pressed ↓ once from row 0 to row 1 (view at top, question visible, no overflow issue)... that's the non-overflow case — wrap is fine.\n\nBut consider the overflow case: question visible at bodyScroll=0, cursor at row 0, user presses ↑ → wrap to last row, view jumps to bottom. The user just lost the question. Is that acceptable? It matches \"bodyScroll == 0 时保持原环绕\" from my plan — the plan explicitly said keep wrap at top. Fine.\n\nNow, one more edge: my ↑ branch requires `this.bodyScroll > 0`. But bodyScroll is only updated during render (clamped). After handleInput → the app calls requestRender. The flow's bodyScroll reflects the last render. In tests, we render after each input. OK.\n\nAlso the ↑ branch when the page does NOT overflow: bodyScroll is always 0 (maxScroll = 0), so ↑ at row 0 wraps. ✓ (non-overflow wrap preserved).\n\nNow let me also think about whether the ↓ branch's `scrolled` should use lastExpandable. lastExpandable = overflow && regionHeight >= 2. If regionHeight = 1 (tiny terminal), no scroll. Fine.\n\nLet me now write the tests. Also I want to double check one thing in the ↑ loop test: when pressing ↑ at row 0 with bodyScroll > 0, I call scrollBody(-1) and return WITHOUT pendingCursorScroll. The cursor stays at row 0. Next render: no follow. bodyScroll = bodyScroll - visible. The cursor row (row 0's content position, say 5) — after scrolling up, viewport = [newBodyScroll, newBodyScroll+visible). If 5 < newBodyScroll (question taller than viewport), cursor invisible. That's the intended \"reading the question\" state. ✓.\n\nTest A trace with fixture: question 'Pick a side' (1 row) + detail longDetail(60) (60 rows) + 6 options + Type something = ~68 rows. Budget 12 → visible ≈ 8ish.\n\n1. 6 ↓ presses: cursor 0→6 (Type something.), view follows to bottom (bodyScroll ≈ 60).\n2. ↑ presses: cursor 6→0 over 6 presses (view follows up as cursor moves up: when cursor row's content position < bodyScroll, follow clamps bodyScroll = cursorFirst — so the view jumps up to the cursor each time; by the time cursor = 0, bodyScroll ≈ cursorFirst(0) ≈ 5? hmm — row 0's content position: question(1) + detail(60) + blank(1) = 62! So row 0 (Option 1) starts at content row 62. When cursor moves up from 6 to 0, the follow keeps the cursor visible: at cursor 0, bodyScroll = 62 - visible + 1 ≈ 55. Viewport [55, 63): shows detail tail + options. Question NOT visible (row 0 at content 0).\n3. ↑ at row 0, bodyScroll=55>0 → scrollBody(-1) → bodyScroll = 55-8 = 47. Question still not visible (content 0 < 47). More ↑: 47→39→31→23→15→7→0 (clamped). At bodyScroll=0, viewport [0,8): question visible → break. ✓ ~7 presses after reaching row 0.\n\nTotal presses ≈ 6 + 6 + 7 = 19 < guard 200. ✓.\n\nNow — WAIT. There's a problem in step 2! When the cursor moves up from row 6 to row 0, the follow clamps bodyScroll = cursorFirst - visible + 1 each time. cursor 5's content position ≈ 62 + (option 5 rows ≈ 2) = 64... hmm wait options are short ('Option N' 1 row each) + no descriptions. So rows: question(1) + detail(60) + blank(1) + 6 options(1 each) + Type something(1) = 69 rows. cursor 0 (Option 1) at content 62. cursor 6 (Type something) at 68.\n\nFollow when moving up: cursor 6→5: cursorFirst(5) = 67. bodyScroll currently ≈ 60 (from the down-follow). 67 >= 60+8=68? No → no clamp (cursor visible in [60,68)). cursor 5→4: cursorFirst(4)=66, bodyScroll 60: visible. ... cursor 1→0: cursorFirst(0) = 62. bodyScroll: after cursor 2→1: cursorFirst(1)=63; 63 >= bodyScroll(60)+8=68? No. So bodyScroll stays 60 through all the up-moves! Cursor 0 at 62, viewport [60,68) — visible. bodyScroll = 60, NOT 55. Then ↑ at row 0: scrollBody(-1): 60-8=52. 52→44→36→28→20→12→4→0 (clamped at 0). At 0: question visible. ✓ ~8 presses.\n\nBut wait — is bodyScroll really 60 at that point? After 6 ↓ presses: cursor 6 (Type something at 68). Follow: 68 >= bodyScroll+visible → bodyScroll = 68-8+1 = 61. maxScroll = 69-8 = 61. bodyScroll = 61. Viewport [61,69). Then ↑ presses: cursor 5 (67): visible in [61,69) ✓ no clamp... cursor 0 (62): visible ✓. bodyScroll stays 61. ↑ at row 0: 61→53→45→37→29→21→13→5→0. ~8 presses. Fine.\n\nOK the test works. Let me also confirm the existing test \"scrollport: PageDown/PageUp page the long body\" still passes — it does (ran already).\n\nNow let me also think about whether the ↑ edge-scroll should happen even when the cursor is NOT at row 0 but the view is scrolled — e.g. cursor at row 2, bodyScroll = 40, cursor invisible above? Can't happen (follow keeps cursor visible). So only row 0 matters for ↑. ✓.\n\nLet me write the tests now. I'll append to question-flow.test.ts.\n\nAlso — I realize I should double check the `scrolled` variable name doesn't shadow anything. In my edit I used `const scrolled = ...` inside the ↓ branch. Fine.\n\nLet me write tests:\n\n```ts\ntest('arrow-key edge scroll: ↑ at the first row scrolls back to the question overview', () => {\n  const f = makeFlow([{\n    id: 'q1',\n    question: 'Pick a side',\n    detail: longDetail(60),\n    options: Array.from({ length: 6 }, (_, i) => ({ label: `Option ${i + 1}` })),\n  }], 12)\n  render(f, 100)\n  // ↓ into the options: the view follows the cursor and the question scrolls away.\n  for (let i = 0; i < 6; i++) f.handleInput('\\x1b[B')\n  assert.ok(!render(f, 100).join('\\n').includes('Pick a side'), `precondition — question must be scrolled away:\\n${render(f, 100).join('\\n')}`)\n  // ↑ repeatedly: the cursor walks back to the first row, then the body scrolls\n  // up page by page until the question overview returns.\n  let guard = 200\n  let back = ''\n  while (guard-- > 0) {\n    f.handleInput('\\x1b[A')\n    back = render(f, 100).join('\\n')\n    if (back.includes('Pick a side')) break\n  }\n  assert.ok(back.includes('Pick a side'), `↑ must scroll back to the question:\\n${back}`)\n})\n\ntest('arrow-key edge scroll: ↓ at the last row with content below scrolls instead of wrapping', () => {\n  const f = makeFlow([{\n    id: 'q1',\n    question: 'Pick a side',\n    detail: longDetail(60),\n    options: Array.from({ length: 6 }, (_, i) => ({ label: `Option ${i + 1}` })),\n  }], 12)\n  render(f, 100)\n  // Move the cursor onto the last row and page the view up so the cursor is\n  // out of view above: the page-up case that leaves bodyScroll below max.\n  for (let i = 0; i < 6; i++) f.handleInput('\\x1b[B')   // cursor to the last row (view at bottom)\n  f.handleInput('\\x1b[5~')                                // page up: view moves up, cursor stays\n  const paged = render(f, 100).join('\\n')\n  assert.ok(paged.includes('↑ '), `precondition — paged up:\\n${paged}`)\n  // ↓ at the last row: scroll the body DOWN toward the cursor instead of\n  // wrapping to the first row.\n  f.handleInput('\\x1b[B')\n  const after = render(f, 100).join('\\n')\n  assert.ok(after !== paged, `↓ must scroll the body down:\\n${after}`)\n  assert.ok(!after.includes('Option 1'), `must not wrap to the first row:\\n${after}`)\n})\n\ntest('arrow-key edge scroll: non-overflow pages keep the wrap-around', () => {\n  const f = makeFlow([{ id: 'q1', question: 'Pick', options: [{ label: 'A' }, { label: 'B' }] }], 24)\n  render(f, 100)\n  f.handleInput('\\x1b[A') // ↑ at the first row: wraps to the last\n  const up = render(f, 100).join('\\n')\n  assert.ok(up.includes('B') && !up.includes('→') || up.includes('→'), ...)\n  ...\n})\n```\n\nHmm the last one: I want to assert the cursor moved to the last row. The pointer `→` is on 'B' now. Simpler: assert the render contains '→ B'? The row is `→ [2] B`. Let me assert `up.includes('[2] B')` hmm, the pointer prefix is `→ [2] B`. I can strip and check the pointer is on B. Actually simpler: count that the render changed and 'A' no longer has the pointer. The existing tests don't do that granularly... Let me just assert that after ↑ at first row (no overflow), the view does NOT scroll (marker absent) and the cursor wrapped to the last row: the joined text contains '→' on the B row. The prefix for selected is `→ [2] `; for unselected `  [1] `. So `up.includes('→ [2]')` works. And `!up.includes('more lines')` — no overflow markers. And ↓ at last row wraps back: `f.handleInput('\\x1b[B'); down.includes('→ [1]')`.\n\nFor the ↓ test above (\"scrolls instead of wrapping\"), the fixture: after paging up, bodyScroll ≈ 61-8 = 53, cursor at last row (content 68). scrolled = lastExpandable(true) && lastContentRows(69) > 53+8(61) → true. → scrollBody(1): 53+8=61 = maxScroll. Viewport [61,69): shows Type something at 68, cursor visible. `after` shows 'Option 6'?? Viewport [61,69) = content rows 61-68: detail rows 60? wait detail is rows 1-60 (longDetail(60) → 60 lines at width 100, each 'detail-NN yyyyy...' — 'y'*70 + prefix ≈ 80 chars < 100 → 1 row each). Content: row 0 = '? Pick a side', rows 1-60 = detail, row 61 = '' blank, rows 62-67 = options 1-6, row 68 = Type something. Viewport [61,69) = blank + options 1-6 + Type something. So 'Option 1' IS in view ([62])! My assertion `!after.includes('Option 1')` would FAIL. Oops.\n\nThe point of the ↓ test is: does it wrap (cursor jumps to row 0, view jumps to top) or scroll? After scroll, viewport = [61,69) — the SAME as before the page-up?? Wait no: before page-up, bodyScroll was 61 (bottom). PageUp: scrollBody(-1) → 53. Viewport [53,61): detail rows 53-60 + blank. Then ↓ at last row: scrolled true → scrollBody(1) → 61. Viewport [61,69). So `after` = bottom view. `paged` = [53,61). They differ. ✓ `after !== paged` holds.\n\nTo assert \"not wrapped\": if it wrapped, cursor → row 0, pendingCursorScroll → bodyScroll = 62 (cursorFirst of row 0) - 8 + 1 = 55. Viewport [55,63): shows detail 55-60, blank, options 1-2. So wrapped view shows 'Option 1' AND 'Option 2' and NOT 'Type something.'. The scroll view [61,69) shows 'Option 1' too (row 62)! Damn — both show Option 1. Distinguish by: scroll view shows 'Type something.' (row 68, at the very bottom edge); wrapped view does NOT show 'Type something.' (row 68 > 62). So assert `after.includes('Type something.')` — that proves the view is at the bottom (not wrapped to top). ✓\n\nLet me use that assertion: after ↓, the view shows the bottom rows ('Type something.' visible), i.e., the body scrolled down rather than wrapping to the top.\n\nAlso check the pointer visibility: after scroll, cursor at last row (content 68), viewport [61,69) → visible at the last row. `after.includes('→')` ✓ (the pointer is on Type something: `→ [7] Type something.`). \n\nOK. Now, also: my ↑ branch — when bodyScroll > 0 — is there a case where the cursor is at row 0 but the follow would be needed after scroll? After scrollBody(-1), if bodyScroll lands such that the cursor row (62) is in view — fine, no follow needed. If above the view — intended. OK.\n\nNow let me also add the pointer-visibility assertion to the ↑ test: during the scroll-up, when the question first becomes visible, the pointer may be out of view — that's fine, don't assert pointer. But per the plan's test list: \"边缘滚动后光标仍在可见区(→ 可见)\". Hmm — my analysis shows that after ↑-edge-scrolling to the question, the pointer is typically NOT visible (it's below the viewport). The plan said \"光标留在首行(它就在可见区)\" — that assumption was wrong for tall questions: the first option row can be below the viewport when the question is tall. But the pointer-visibility guarantee is preserved for the ↓ direction (last row is last content row). For ↑, the pointer leaving the view is CORRECT UX (reading the question). I'll adjust the test to assert what matters: the question returns. And keep the existing \"cursor movement keeps the pointer in view\" test (↓ direction) green.\n\nActually — let me reconsider the ↑ case once more for UX: user at options (view scrolled), presses ↑ to read the question. View scrolls up; question appears; pointer out of view. User wants to go back to the options: presses ↓. Cursor is at row 0 (first option), atLastRow false → cursor 1, pendingCursorScroll → view jumps to show row 1 (scrolls down to the options). ✓ The return path works even with the pointer out of view. \n\nNow write the tests. Also, per the plan, mark todo 1 complete after tests pass. Let me add the tests to question-flow.test.ts."}}}}
